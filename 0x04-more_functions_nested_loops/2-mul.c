#include "main.h"
#include <stdio.h>

/**
* mul - returns the product of two ints
* @c: int given by the main function
* @d: int given by the main function
* Description: uses header to link and nest loops
* Return: Returns two ints
*/

int mul(int c, int d)
{
	return (c * d);
}
